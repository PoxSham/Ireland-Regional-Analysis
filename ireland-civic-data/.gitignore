# Raw API responses — large, regenerated on each ETL run
data/raw/

# Processed data files — large, regenerated by ETL
# Manifests and run_report are tracked (small, informative)
data/processed/cso_*.json
!data/processed/cso_manifest.json
data/processed/eurostat_*.json
!data/processed/eurostat_manifest.json
data/processed/ogp_*.json
!data/processed/ogp_manifest.json

# Snapshots — frozen builds (tracked selectively when needed)
data/snapshots/

# Node
node_modules/
